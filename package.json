{
  "name": "disposable-email-reader",
  "version": "0.0.2",
  "description": "A simple wrapper for some disposable email APIs.",
  "main": "lib/index",
  "types": "lib/index",
  "scripts": {
    "start": "npm run build:live",
    "build": "./node_modules/.bin/tsc",
    "build:live": "./node_modules/.bin/tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "disposable",
    "email"
  ],
  "author": "Alejandro D. Simi",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^8.0.12",
    "typescript": "^2.4.1"
  },
  "dependencies": {
    "rxjs": "^5.4.2",
    "unirest": "^0.5.1"
  }
}
